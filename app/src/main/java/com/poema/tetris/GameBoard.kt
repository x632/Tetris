package com.poema.tetris

object GameBoard {
    var arr = Array(20) { Array<Int>(12) {0} }

/*    fun emptyArray(){
       arr.forEachIndexed {y, row ->
           row.forEachIndexed{
                   x, value ->
               arr[y][x] = 0
           }
       }

    }*/
}